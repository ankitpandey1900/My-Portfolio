import type { SolarSystemConfig } from './solar-system-types';

export const DEFAULT_SOLAR_CONFIG: SolarSystemConfig = {
  timeScale: 1.15,
  orbitSpeedMultiplier: 1.65,
  accessibilityMode: false,
  theme: 'cinematic',
  difficulty: 'normal',
};

